{
  "name": "vscode-sops",
  "displayName": "vscode-spos",
  "description": "Vscode extension for encrypt and decrypt files with sops on the fly ",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.83.0"
  },
  "activationEvents": [
    "*"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
    ]
  },
  "scripts": {
    "vscode:prepublish": "tsc -p ./"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.101.0",
    "typescript": "^5.0.0"
  }
}
